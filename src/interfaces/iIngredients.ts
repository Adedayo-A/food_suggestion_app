export interface iIngredients {
    id: string;
    name: string;
    image: string;
    verified: boolean
}

export interface iingredient {
    id: number
    name: string
    imageUrl: string
    created: string
}
